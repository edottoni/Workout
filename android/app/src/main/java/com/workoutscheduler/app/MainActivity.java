package com.workoutscheduler.app;

import com.getcapacitor.BridgeActivity;

public class MainActivity extends BridgeActivity {
    // All plugins are registered automatically via each plugin's own
    // @CapacitorPlugin annotation + capacitor.build.gradle. No manual
    // registerPlugin() calls are required for the plugins used here
    // (@capacitor/app, @capacitor/keyboard, @capacitor/splash-screen,
    // @capacitor/status-bar).
}
