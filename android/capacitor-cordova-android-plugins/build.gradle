// DO NOT EDIT THIS FILE BY HAND. It is (re)generated by `npx cap sync android`.
// It aggregates any Cordova-style plugins used by installed Capacitor plugins.
// This project currently has none, so the module below is intentionally empty.
ext {
    postBuildExtras = {}
}

apply plugin: 'com.android.library'

android {
    namespace "com.workoutscheduler.app.cordova.plugins"
    compileSdk project.hasProperty('compileSdkVersion') ? rootProject.ext.compileSdkVersion : 35
    defaultConfig {
        minSdkVersion project.hasProperty('minSdkVersion') ? rootProject.ext.minSdkVersion : 23
        targetSdkVersion project.hasProperty('targetSdkVersion') ? rootProject.ext.targetSdkVersion : 35
    }
    lintOptions {
        abortOnError false
    }
}

dependencies {
}
